{
  "expected_current_sha": "b3a45b0c98623f6534a55c899d8ed2babae7d378",
  "target_sha": "4daf826162ad531fa6963c019dbf937589952972",
  "reason": "Publish #191, which stops the gate-typescript workflow deriving its runner tier from repository language and makes it read the consumer's DECLARED Workload Class instead (standards ADR-0022, shipped in template/v1.55.50). Gate Family surface of this transition is exactly one file: audit.yml, lint-format.yml, secret-scan.yml and pr-title.yml are byte-identical at b3a45b0 and at 4daf826, so typecheck-ts.yml is the only ruleset-consumed blob in the published range. The other 22 commits are inert to this tag - release-please releases, copier re-renders, a plugin-carrier sync, Renovate preset edits (#176/#182/#185/#188), a PR-template adoption, and #167/#175 which touch lint-hooks.yml and bump-brew.yml, thin-called reusables that consumers pin by SHA off the floating v3 and that no ruleset reads from gates/wf-v1. WHAT ACTUALLY CHANGES FOR THE FLEET, stated against live state rather than against main: the LIVE gate at b3a45b0 runs typecheck on `fromJSON(vars.RUNNER_GLUE || '[\"ubuntu-slim\"]')` with no heavy tier anywhere in the file. #167's unconditional glue-heavy version was landed but never published, so it is superseded here and never reaches the fleet - the desired outcome, because routing every gate-typescript repository to a 3-slot pool is precisely the over-provisioning ADR-0022 exists to remove. After this publication the file gains a `route` job that sparse-checks out .copier-answers.yml, greps typecheck_workload_class, and resolves glue-heavy through the chain RUNNER_GLUE_HEAVY || RUNNER_GLUE || ubuntu-slim; typecheck then runs on `needs.route.outputs.runner`. RUNNER_GLUE_HEAVY does not exist (deliberately absent from .github-private runners.tf until the glue-heavy pool converges; mac-dev-playbook#497 is merged but not yet deployed), and no repository declares glue-heavy on its default branch today, so every selected repository resolves to RUNNER_GLUE exactly as it does now. The observable delta for the fleet is one short additional glue job per typecheck run. The `gate` bucket now depends on [route, typecheck] rather than [typecheck] alone: a failed route SKIPS typecheck, and `skipped` matches neither failure nor cancelled in the aggregate, so without that edge the required check would have reported PASS while nothing was typechecked. That is a fail-closed correction, not a loosening. Candidate Validation judgment: a temporary evaluate-mode duplicate ruleset at the candidate was considered and NOT performed, and the reasoning is deliberately NOT #173's - this change IS structural, so that request's 'additive diff, no new execution path' argument does not apply and is not reused. Four grounds. (1) The candidate job graph executed end to end on real runners, twice, in this repository's own PR checks: #191 run 30865574411 (route job 91856562563 success, typecheck 91856585200 success) and #192 run 30865818236 (route 91857308766 success, typecheck 91857360489 success), with the gate bucket green in both. The new job, the new needs edge, and the fromJSON(needs.route.outputs.runner) indirection are observed working rather than assumed. (2) The route job is a direct copy of lint-hooks.yml's `detect` job, which performs the same sparse-checkout-then-grep-then-emit-runner-JSON on every consumer pull request across the fleet today, and did so successfully on kodus-ai as recently as 2026-08-04T01:15Z (job 91861413701). The mechanism carrying the residual risk - a job emitting a JSON array string that a downstream runs-on consumes through fromJSON - is the most exercised pattern in this repository. (3) The transition is behaviour-preserving under current live state, as established above: same resolved runner, same steps, for every selected repository. An evaluate-mode duplicate would observe RUNNER_GLUE, which is what the enforced rule already observes. (4) The blast radius is bounded in the fail-closed direction: the failure mode is a red required check on gate-typescript repositories, not a silently passing one. HONEST GAPS, so this is not read as a stronger claim than it is: the glue-heavy branch of the route job is NOT exercised by any CI run in evidence - .github ships no .copier-answers.yml, so both PR runs took the else branch - and the route job has not run against a real consumer answers file in THIS workflow. The glue-heavy predicate was instead verified out-of-band against ten input spellings (bare, double-quoted, single-quoted, uppercase, trailing whitespace, the prefix glue-heavy-xl, the sibling lint_hooks_workload_class key, has_typescript alone, a commented-out line, and an absent file), with only the four genuine spellings routing heavy. That gap is acceptable because the branch is unreachable in production until both RUNNER_GLUE_HEAVY exists and some repository declares the class, and neither is true at publication time; it becomes reachable only under the deliberate, operator-controlled sequence in mac-dev-playbook#497's deploy ordering. Rollback is cheap and unchanged: another request-only PR setting target_sha back to b3a45b0c98623f6534a55c899d8ed2babae7d378 with expected_current_sha reread from the live ref.",
  "references": [
    "https://github.com/Rubio-Enterprises/.github/pull/191",
    "https://github.com/Rubio-Enterprises/.github/actions/runs/30865574411",
    "https://github.com/Rubio-Enterprises/.github/actions/runs/30865818236",
    "https://github.com/Rubio-Enterprises/standards/blob/main/docs/adr/0022-glue-heavy-workload-class.md",
    "https://github.com/Rubio-Enterprises/standards/pull/450",
    "https://github.com/Rubio-Enterprises/mac-dev-playbook/pull/497"
  ]
}
